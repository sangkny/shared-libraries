# shared-libraries/requirements.txt

# ── 핵심 (항상 필요) ──────────────────────────────────────
openai>=1.30.0              # Local(LM Studio) + OpenAI + Azure Provider
httpx>=0.27.0               # health_check, HTTP 클라이언트

# ── LLM Provider (선택적) ─────────────────────────────────
anthropic>=0.28.0           # Anthropic Claude
google-genai>=1.0.0         # Google Gemini (신규 패키지 — generativeai deprecated)

# ── 유틸리티 ─────────────────────────────────────────────
python-dotenv>=1.0.0        # .env 파일 로드

# ── 테스트 ───────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0      # async 테스트 지원
pytest-cov>=5.0.0           # 커버리지 측정
