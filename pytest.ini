# shared-libraries/pytest.ini
[pytest]
asyncio_mode = auto
testpaths =
    tests
addopts =
    -v
    --tb=short
    --no-header
log_cli = true
log_cli_level = WARNING
# agents/tests, llm/tests 는 상대 import를 사용하므로
# tests/test_integration.py 와 tests/test_harness.py 로 통합 관리
