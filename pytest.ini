# shared-libraries/pytest.ini
[pytest]
asyncio_mode = auto
testpaths =
    ontology/tests
    agents/tests
    llm/tests
addopts =
    -v
    --tb=short
    --no-header
log_cli = true
log_cli_level = INFO
